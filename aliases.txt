alias stant="$HOME/Documents/stant"
alias estudos="$HOME/Documents/estudos"
alias getbranch="git branch | grep \* | cut -d ' ' -f2 | pbcopy"
alias getdir="pwd | pbcopy"
alias p="pod"
alias tree="find . -print | sed -e 's;[^/]*/;|____;g;s;____|; |;g'"
alias gethistory="history | >> history.txt"
alias fetch="git fetch &"
alias rmbranches="git fetch -p && for branch in `git branch -vv | grep ': gone]' | awk '{print $1}'`; do git branch -D $branch; done"
alias startover="git checkout sandbox && g fetch -q && g pull"
